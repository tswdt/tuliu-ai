# tuliu-ai 项目总结

## 1. 技术栈

`tuliu-ai` 项目主要基于 **Next.js** 框架构建，结合了现代 Web 开发的多种技术。以下是其核心技术栈：

| 类别       | 技术             | 描述                                                               |
| :--------- | :--------------- | :----------------------------------------------------------------- |
| **前端框架** | Next.js          | 基于 React 的全栈框架，用于构建用户界面和处理服务器端逻辑。        |
| **UI 库**    | React            | 用于构建交互式用户界面的 JavaScript 库。                           |
| **样式**     | TailwindCSS      | 实用至上的 CSS 框架，用于快速构建自定义设计。                      |
| **语言**     | TypeScript       | JavaScript 的超集，提供类型安全和更好的开发体验。                  |
| **数据库**   | 无               | 严格遵循 Serverless 架构，不使用传统数据库。                       |
| **对象存储** | Tencent COS      | 腾讯云对象存储，用于存储上传的图片、处理中间结果和最终生成结果。   |
| **AI 服务**  | SiliconFlow      | 提供 AI 模型调用服务，包括：                                       |
|            | - Hunyuan Vision | 用于产品图像的视觉分析，生成商业摄影提示。                         |
|            | - Flux Fill      | 用于图像的场景重绘，实现电商级视觉详情页的生成。                   |
|            | Bria Matting     | 专业的智能抠图服务，用于从产品图中精确分离产品主体。               |
| **安全**     | Cloudflare Turnstile | 用于验证用户请求，防止机器人和滥用。                               |
| **状态管理** | Sonner           | 用于显示 toast 提示。                                              |
| **工具库**   | Zod              | 用于运行时类型验证。                                               |
|            | clsx, tailwind-merge | 用于条件性地组合 CSS 类名。                                        |

## 2. 当前架构状态

`tuliu-ai` 采用了一种**无服务器（Serverless）**的架构模式，利用 Next.js 的服务器组件（Server Components）和 API 路由来处理后端逻辑，并与多个外部 AI 服务和云存储服务集成。其核心架构可以概括为以下几个部分：

1.  **前端界面 (Next.js / React)**：
    *   用户通过 `app/page.tsx` 提供的界面上传产品图片。
    *   界面展示任务处理进度、日志和最终生成结果。
    *   使用 `ImageComparison` 组件展示处理前后的图片对比。
    *   通过轮询 `/api/poll` 接口获取任务的最新状态。

2.  **后端逻辑 (Next.js Server Actions / API Routes)**：
    *   `server/actions/generate.ts` 包含了核心的生成流程逻辑，通过 `startGeneration` 函数触发。
    *   `/api/poll/route.ts` 提供了一个 API 接口，供前端轮询查询任务状态。
    *   `lib/services` 目录下封装了与外部服务交互的逻辑，包括 AI 服务 (`ai.ts`)、对象存储 (`cos.ts`)、任务状态管理 (`job.ts`) 和安全验证 (`security.ts`)。

3.  **任务管理 (COS & JobState)**：
    *   任务状态 (`JobState`) 不存储在传统数据库中，而是以 JSON 文件的形式存储在 **Tencent COS** 上，路径为 `jobs/{jobId}.json`。这种方式体现了无服务器架构的特点，利用云存储作为轻量级的状态持久化方案。
    *   `lib/services/job.ts` 负责任务状态的初始化、更新和获取。
    *   COS 配置了生命周期规则，自动删除 `jobs/` 前缀下的文件（7天后），以管理存储成本和数据生命周期。

4.  **AI 管道 (AI Pipeline)**：
    *   整个系统围绕一个多步骤的 AI 处理管道构建，每个步骤都调用不同的外部 AI 服务。
    *   通过 `lib/utils/retry.ts` 实现了对外部 API 调用的重试和超时机制，增强了系统的健壮性。

5. **数据存储 (无数据库)**：
    *   项目已移除 Prisma 和 SQLite，确保完全符合 Serverless 部署要求。所有持久化需求均通过腾讯云 COS 实现。。

## 3. 系统已实现功能

根据代码分析，`tuliu-ai` 系统已实现以下核心功能：

1.  **产品图片上传**：用户可以通过前端界面上传产品图片，支持图片预览和大小限制（10MB）。
2.  **任务启动与管理**：
    *   为每个上传图片生成一个唯一的 `jobId`。
    *   通过 `startGeneration` 函数启动图片处理任务。
    *   任务状态（`pending`, `analyzing`, `matting`, `generating`, `completed`, `failed`）和进度通过 COS 上的 JSON 文件进行持久化和更新。
    *   前端通过轮询机制实时显示任务状态、进度和日志。
3.  **多步骤 AI 图像处理流程**：
    *   **安全验证**：通过 Cloudflare Turnstile 验证用户请求的合法性。
    *   **视觉分析 (Hunyuan Vision)**：分析上传的产品图片，生成详细的商业摄影提示。
    *   **内容审核**：对视觉分析结果进行内容审核，确保生成内容符合规范（当前为模拟实现）。
    *   **智能抠图 (Bria Matting)**：精确地从产品图中抠出产品主体，生成掩码图片。
    *   **场景重绘 (Flux Fill)**：结合原始图片、抠图掩码和视觉分析提示，生成具有电商级视觉效果的详情页图片。
4.  **结果展示与下载**：任务完成后，系统会显示最终生成的图片，并提供下载高清图的选项。
5.  **错误处理与日志记录**：任务执行过程中，任何步骤失败都会更新任务状态为 `failed`，并记录错误信息和详细日志，方便问题排查。
6.  **幂等性与任务恢复**：系统在启动任务时会检查现有任务状态，如果任务已完成，则直接返回结果，支持任务的幂等性和从中断处恢复。
7.  **腾讯云 COS 集成**：利用 COS 进行文件上传、下载和任务状态的存储，并配置了生命周期管理规则，自动清理过期数据。

总体而言，该项目已经构建了一个从用户上传到 AI 处理再到结果展示的完整工作流，核心的 AI 图像处理管道已基本成型，并考虑了任务状态持久化、重试机制和安全验证等关键方面。
