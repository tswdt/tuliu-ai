# 图流 AI (Tuliu AI) 技术架构分析报告

## 1. 架构概述
图流 AI 采用 **Serverless First** 的设计理念，完全基于腾讯云生态构建。系统移除了传统的数据库（如 Prisma/MySQL），转而使用 **腾讯云对象存储 (COS)** 作为全局状态机和持久化层。这种架构极大地降低了运维成本，并实现了天然的水平扩展能力。

## 2. 核心系统实现

### 2.1 钱包系统 (Wallet System)
钱包系统实现了基于 COS 的轻量级账本功能。
- **存储路径**: `users/{userId}/wallet.json`
- **数据结构**: 包含 `balance` (余额) 和 `transactions` (交易流水)。
- **原子性保证**: 虽然 COS 不是关系型数据库，但通过 Serverless 函数的串行逻辑，实现了“读取-校验-扣费-写回”的闭环。
- **接口设计**:
    - `getBalance(userId)`: 获取当前余额，默认为 0。
    - `deductCredit(userId, amount)`: 扣费逻辑，余额不足时抛出异常，中断生成流程。

### 2.2 内容安全系统 (Real Content Safety)
系统集成了腾讯云原生安全能力，替换了原有的 Mock 逻辑，确保商业化运营的合规性。
- **技术栈**: `tencentcloud-sdk-nodejs` (TMS & IMS)。
- **校验维度**:
    - **图片审核 (IMS)**: 在用户上传原图后立即触发，拦截色情、恐怖、政治敏感等违规图片。
    - **文本审核 (TMS)**: 对 AI 生成的视觉描述（Prompt）进行二次校验，防止模型产生违规文本。
- **安全策略**: 采用 **Fail-closed** 策略，即如果安全 API 调用失败或超时，系统默认拒绝访问，确保零风险。

### 2.3 任务流水线集成
在 `server/actions/generate.ts` 中，我们重新编排了生成流转逻辑：
1. **前置校验**: Turnstile 人机验证。
2. **安全准入**: IMS 图片审核。
3. **商业准入**: 钱包扣费（1 积分/次）。
4. **视觉分析**: Hunyuan Vision 分析。
5. **二次安全**: TMS 文本审核。
6. **后续处理**: Bria 抠图 -> Flux 场景重绘。

## 3. 架构优势
- **极简运维**: 无需维护数据库集群，所有状态均在 COS 中可见、可溯源。
- **合规性**: 深度集成腾讯云安全 SDK，满足国内监管要求。
- **成本优化**: 按量计费，无生成任务时几乎零成本。

## 4. 后续建议
- **并发控制**: 随着用户量增长，建议引入 COS 的 `If-Match` (ETag) 机制来处理钱包并发扣费的竞态问题。
- **异步化**: 对于耗时较长的安全校验，可以考虑进一步优化为异步预校验。
